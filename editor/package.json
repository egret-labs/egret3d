{
  "name": "@egret/inspector",
  "version": "1.5.1",
  "compilerVersion": "5.3.2",
  "description": "egret 3d inspector lib",
  "main": "bin/inspector.js",
  "bin": {},
  "scripts": {
    "build": "egret build && node ./script/build.js",
    "build-examples": "egret build && node ./script/build.js && cd .. && cd examples && egret clean && cd .. && cd editor"
  },
  "author": "egret",
  "license": "ISC",
  "dependencies": {
    "@types/stats": "^0.16.30"
  }
}