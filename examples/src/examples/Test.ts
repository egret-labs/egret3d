namespace Test {
    export async function start() {
        // Create camera.
        egret3d.Camera.main;

        const abc = egret3d.DefaultMeshes.createObject(egret3d.DefaultMeshes.CUBE_LINE);

        console.log("Test");
    }
}